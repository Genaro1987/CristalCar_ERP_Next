-- 039_CORE_TELA_AJUDA_ATUALIZACOES.sql
-- Migration intencionalmente sem efeito (NO-OP).
-- Os ajustes de ajuda das telas serão tratados em uma migration posterior (040),
-- para evitar novos erros de parse em strings longas.

SELECT 1 AS DUMMY;
